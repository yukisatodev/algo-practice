"""
問題: 与えられた文字列が回文（前から読んでも後ろから読んでも同じ）かどうかを判定する。
入力: 文字列 S（1行）
出力: 回文なら "Yes"、そうでなければ "No"

メモ: S == S[::-1] で前後を比較するのが最短。
      スペースや大文字小文字を無視したい場合は、比較前に正規化する。
"""


def solve():
    s = input().strip()
    print("Yes" if s == s[::-1] else "No")


if __name__ == "__main__":
    solve()
